# CLAUDE.md

이 저장소에서 작업할 때 반드시 먼저 읽는다.

## 이 저장소는 무엇인가

멀티캠퍼스 A.IM 1회차 부트캠프의 TIL(학습 기록) 저장소.
**이 폴더 자체가 옵시디언 보관함(vault)이자 git 저장소다.** 같은 파일을 옵시디언이 편집하고 git이 추적한다.

```
TIL/            학습 기록 (YYYY-MM-DD_주제.md)
attachments/    이미지
_templates/     옵시디언 템플릿
.claude/        이 파일과 슬래시 명령
복습_확인질문.md   각 TIL의 확인 질문을 모아보는 노트
옵시디언_사용법.md  사용자용 가이드
```

## 사용자 상황

- 개발 입문자. 부트캠프 수강 중. 옵시디언·git 모두 초보다.
- TIL을 쓰는 목적은 **깃허브 잔디 + 시험공부 복습** 두 가지다.
- 하루 9시간 수업. 수업 중에는 Gemini와 대화하며 학습하고, 한 시간마다 정리본을 받아 파일에 붙여넣는다.
- 강사가 언제 주제를 끝낼지 알 수 없어 **같은 주제가 여러 조각으로 흩어진다.** 이게 이 저장소의 핵심 문제다.
- Gemini는 무료 티어라 긴 글을 요약해버린다. 그래서 재구성 작업은 Claude가 맡는다.

## 절대 규칙

### 1. 요약하지 않는다

TIL 파일을 정리할 때 **분량을 줄이는 것은 실패다.**
설명을 짧게 만들거나, 덜 중요해 보이는 것을 쳐내지 않는다.
정리 후 분량이 원본의 80% 미만이면 되돌리고 다시 한다.

**단, 문장은 다듬는다.** 이 저장소의 목적은 기록 보존이 아니라 **배운 것을 시험공부에 쓸 수 있게 만드는 것**이다.

- 서술형으로 통일한다: `~함` `~하는것` `~라고함` → `~한다` `~하는 것이다`
- 명백한 오타·오기는 고친다. (예: 대채→대체, 횝사이트→웹사이트)
- 비문과 중복 표현을 정리해 읽히는 문장으로 만든다.
- **줄이는 게 아니라 고쳐 쓰는 것이다.** 문장을 다듬다가 정보가 빠지면 규칙 위반이다.

### 1-2. 틀린 내용은 고치고, 알면 좋은 것은 덧붙인다

수업 필기에는 사용자가 잘못 알아들은 내용이 섞여 있다. 그대로 두면 시험공부에 해롭다.

**틀린 내용**은 올바르게 고치고, 그 자리에 한 줄을 남긴다:

```markdown
> ♻️ 처음엔 (틀린 이해)로 알았으나, (맞는 내용)이 맞다.
```

**보강할 내용**은 본문에 섞지 말고 반드시 별도 블록으로 분리한다:

```markdown
> ➕ **더 알아두기**
> (수업에서 안 나왔지만 알아두면 좋은 내용)
```

이유: 사용자는 이 노트로 시험을 준비한다. **강사가 실제로 말한 것**과 **Claude가 보탠 것**이 구분되지 않으면 시험 범위를 알 수 없다.
본문에 자연스럽게 녹여 쓰지 말 것. 확신이 없는 내용은 아예 쓰지 말고 사용자에게 묻는다.

### 2. 이미지 링크는 표준 마크다운

```
✅ ![](../attachments/파일명.png)      GitHub에서 보인다
❌ ![[파일명.png]]                      GitHub에서 안 보인다
```

공백은 `%20`으로 인코딩되어야 한다. 기존 이미지 링크의 경로는 절대 건드리지 않는다.

**예외**: `복습_확인질문.md` 안의 `![[파일#제목]]` 임베드는 의도된 것이다. 옵시디언 전용 기능을 일부러 쓰는 자리다. 고치지 말 것.

### 3. 제목 레벨

파일당 `#`은 맨 위 하나뿐. 문서 안 주제는 `##`, 소주제는 `###`.
`#`이 여러 개면 GitHub와 옵시디언 양쪽에서 목차가 깨진다.

### 4. 코드블록

- 열었으면 반드시 닫는다. 닫히지 않은 코드펜스 때문에 문서 한 섹션이 통째로 회색 덩어리가 된 적이 있다.
- 코드블록 **안의 내용은 절대 수정하지 않는다.**

### 5. 커밋

옵시디언 Git 플러그인이 편집 후 5분에 자동 커밋·푸시한다.
**요청받지 않으면 커밋하지 않는다.** 파일만 고치고 사용자에게 알린다.

## 자주 하는 작업

### TIL 정리

`/til` 명령을 쓴다. 정의는 `.claude/commands/til.md` 에 있다.
슬래시 명령을 쓸 수 없는 상황이면 그 파일을 읽고 내용대로 수행한다.

### 새 TIL 작성 지원

파일명은 `TIL/YYYY-MM-DD_주제.md`. 날짜순 정렬을 위해 날짜가 앞에 온다.

## 응답 방식

- 한국어로 답한다.
- 사용자는 입문자다. 전문 용어를 쓰면 **그 자리에서 한 줄로 뜻을 붙인다.**
- 되돌릴 수 없는 작업(파일 삭제, 브랜치 삭제, 강제 푸시) 전에는 반드시 묻는다.
- 파일을 크게 고쳤으면 **변경 전후 글자 수를 보고한다.** 사용자가 손실 여부를 확인할 수 있어야 한다.
- 추측으로 답하지 않는다. 파일 내용이 필요하면 읽고 확인한 뒤 답한다.
