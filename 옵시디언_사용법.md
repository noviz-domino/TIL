# 🧭 옵시디언 사용법

> 이 저장소를 쓰다가 막힐 때 보는 문서입니다.
> 이 파일 자체도 마크다운이니, 편집 모드(`Ctrl+E`)로 열어보면 문법 예시가 그대로 보입니다.

---

## 1. 기본 개념 3줄

- **보관함(vault)** = 그냥 폴더입니다. 여기서는 `Desktop\TIL` 폴더가 보관함입니다.
- 옵시디언은 그 폴더 안의 `.md` 파일을 **직접 열어서 편집**합니다. 별도 저장 공간이 없습니다.
- 그 폴더가 곧 GitHub 저장소라서, 쓰면 그대로 올라갑니다.

---

## 2. 매일 쓰는 흐름

1. 옵시디언 켜기 → 자동으로 GitHub 최신 내용을 받아옴
2. `Ctrl + N` → 새 노트 (자동으로 `TIL/` 폴더에 생김)
3. 파일명을 `2026-08-01_주제.md` 형식으로 변경
4. 템플릿 삽입 (아래 4번 항목 참고)
5. 수업 들으며 Gemini와 대화 → 한 시간마다 정리본 붙여넣기 (**지금까지 하던 그대로**)
6. 수업 끝나고 Claude Code에서 **`/til`** 실행 → 주제별 재구성 + 확인 질문 생성
7. 편집 멈추고 5분 뒤 자동 커밋 · 바로 끌 땐 `Ctrl + Shift + Q`

---

## 3. 자주 쓰는 단축키

| 키 | 기능 |
|---|---|
| `Ctrl + N` | 새 노트 |
| `Ctrl + O` | 파일 이름으로 빠르게 찾아 열기 |
| `Ctrl + Shift + F` | **보관함 전체 검색** (시험공부할 때 이걸 제일 많이 씁니다) |
| `Ctrl + F` | 지금 문서 안에서 찾기 |
| `Ctrl + E` | 편집 모드 ↔ 읽기 모드 전환 |
| `Ctrl + P` | 명령 팔레트 (모든 기능을 이름으로 실행) |
| `Ctrl + V` | 캡처한 이미지 붙여넣기 → `attachments/` 에 자동 저장 |
| `Ctrl + Shift + Q` | 커밋 + 푸시 + 종료 |

> 뭔가 하고 싶은데 방법을 모르겠으면 일단 `Ctrl + P` 를 누르고 한국어로 검색해보세요.
> 대부분의 기능이 거기 있습니다.

### 개요(Outline) 패널 켜기

`Ctrl + P` → `개요` 검색 → 실행하면 오른쪽에 **제목 목록**이 뜹니다.
3만 자짜리 문서도 여기서 클릭 한 번으로 원하는 주제로 점프할 수 있습니다.

---

## 4. 템플릿 쓰기

`_templates/TIL.md` 에 템플릿이 들어 있습니다.

**최초 1회 설정**
1. `설정 ⚙️` → `코어 플러그인` → **`템플릿`** 켜기
2. `설정 ⚙️` → `템플릿` → `템플릿 폴더 위치`에 `_templates` 입력
3. `설정 ⚙️` → `단축키` → `템플릿: 템플릿 삽입` 검색 → `Alt + T` 등으로 등록

**사용법**: 새 노트에서 `Alt + T` → `TIL` 선택

---

## 5. 마크다운 문법 (GitHub에서도 잘 보이는 것들)

````text
## 제목 2단계        ← 문서 안 주제는 ## 부터 시작
### 제목 3단계

**굵게**   *기울임*   `인라인 코드`

- 목록
  - 들여쓴 목록
1. 번호 목록

- [ ] 할 일
- [x] 끝낸 일

> 인용문

| 항목 | 설명 |
|---|---|
| 표 | 이렇게 씁니다 |

[링크 텍스트](https://example.com)

```python
print("코드 블록: 언어 이름을 적으면 색이 입혀집니다")
```

---  ← 구분선
````

### 접어두기 (긴 내용 숨기기)

````text
<details>
<summary>클릭하면 펼쳐집니다</summary>

여기에 긴 내용

</details>
````

### 다이어그램 (mermaid)

옵시디언과 GitHub **둘 다** 그림으로 그려줍니다.

````text
```mermaid
flowchart LR
    A[클라이언트] -->|요청| B[서버]
    B -->|응답| A
```
````

---

## 6. 수업 끝나고 `/til` 로 정리하기

### 수업 시작할 때 — Gemini 세팅

새 Gemini 대화창을 열고 [제미나이_시작_프롬프트](제미나이_시작_프롬프트.md) 의 블록을 붙여넣으세요.
하루 한 번만 하면 됩니다.

### 수업 중에는 그냥 붙여넣기만

대화하다가 `정리해줘` 하고, 결과를 **그대로 붙여넣으세요.**
형식을 맞추려 애쓰지 않아도 됩니다. 시간순으로 뒤죽박죽이어도 괜찮습니다.

**요청 간격은 자유입니다.** 20분마다 해도 되고 두 시간에 한 번 해도 됩니다.
Gemini가 직전 정리 이후 새로 나온 부분만 써주므로 내용이 겹치지 않습니다.
새로 나온 게 없으면 "새로 나온 내용이 없어"라고 답할 텐데, 그럼 붙여넣지 않으면 됩니다.

강사님이 언제 주제를 끝낼지 알 수 없으니 정리가 조각나는 건 당연합니다.
그건 나중에 기계가 합칩니다.

### 수업 끝나고 한 번

이 폴더에서 Claude Code를 열고:

```text
/til
```

오늘 날짜 파일을 알아서 찾아 이렇게 바꿔줍니다.

| 하는 일 | 설명 |
|---|---|
| 주제별 재배치 | 흩어진 같은 주제를 하나로 합침 |
| 제목 레벨 정리 | `#` 여러 개 → `##` 으로 통일 (목차가 살아남) |
| 태그 추가 | 맨 위에 `tags: [http, dns]` |
| 관련 글 연결 | 같은 주제의 이전 TIL로 링크 |
| 확인 질문 5개 | 이해도 점검용. 답은 없음 |
| 행정 내용 분리 | 출석 안내·팀 회의는 맨 아래 별도 섹션으로 |
| 복습 노트 갱신 | `복습_확인질문.md` 에 오늘 질문 추가 |

> **요약이 아니라 재배치입니다.** 분량은 그대로 유지되고 순서와 구조만 바뀝니다.
> 정리 전 버전은 git에 남으니 마음에 안 들면 되돌릴 수 있습니다.

특정 파일을 지정하려면 `/til 2026-07-28_n8n_2일차_실습.md` 처럼 씁니다.

---

## 6-1. 복습하는 법

시험공부는 [[복습_확인질문]] 노트 하나만 열면 됩니다.

각 날짜의 확인 질문이 **한 페이지에 전부 펼쳐져** 보입니다. 파일을 하나씩 열 필요가 없습니다.

1. 위에서부터 질문을 읽고 **소리 내어 답해봅니다**
2. 막히면 그 주제의 원본 글로 이동
3. 두 번 이상 막힌 건 노트 위쪽 `🔁 두 번 이상 막힌 것` 에 적어둡니다

시험 전날엔 그 목록만 보면 됩니다.

> 눈으로 읽으면 다 아는 것 같습니다. 말로 설명하려 하면 그때 드러납니다.

---

## 7. 나중에 다시 찾기 (시험공부용)

### 전체 검색 — `Ctrl + Shift + F`

```text
stateless          단어가 들어간 모든 글
tag:#http          http 태그가 붙은 글 전부
path:TIL 쿠키      TIL 폴더 안에서 "쿠키" 검색
```

### 태그

글 맨 위 frontmatter에 이렇게 적습니다. (Gemini가 붙여줍니다)

```yaml
---
tags: [http, network, api]
---
```

읽기 모드에서 태그를 **클릭**하면 같은 태그가 붙은 글이 전부 모입니다.

### 글끼리 연결 — 옵시디언의 진짜 장점

`[[` 를 입력하면 파일 목록이 뜹니다. 고르면 링크가 만들어집니다.
`/til` 명령이 매번 `🔗 관련 글` 섹션에 이전 글을 자동으로 연결해줍니다.

**백링크**
글을 열고 오른쪽 패널에서 확인하거나 `Ctrl + P` → `백링크` 로 켭니다.
*이 글을 참조하는 다른 글* 목록이 뜹니다. HTTP 글을 열면 HTTP를 언급한 모든 날의 글이 보입니다.

**그래프 뷰**
`Ctrl + P` → `그래프 뷰` 를 실행하면 글이 점, 링크가 선으로 그려집니다.
시간이 지나면 주제별로 뭉친 덩어리가 눈에 보입니다.
어떤 주제를 많이 다뤘고 어떤 게 외톨이인지 한눈에 파악됩니다.

> 노션에는 없던 기능입니다. 글이 30개쯤 쌓이면 체감됩니다.

---

## 8. 문제가 생겼을 때

### GitHub에 안 올라간 것 같을 때

1. 우측 하단 **상태바** 확인 → 숫자가 있으면 아직 커밋 안 된 것
2. `Ctrl + P` → `Commit-and-sync` 실행
3. 그래도 안 되면 `Ctrl + P` → `Open source control view` 에서 오류 메시지 확인

### 사진이 GitHub에서 안 보일 때

편집 모드로 열어서 이미지 문법을 확인하세요.

```text
✅ ![](../attachments/파일명.png)      ← 이건 GitHub에서 보임
❌ ![[파일명.png]]                     ← 이건 옵시디언에서만 보임
```

`❌` 형태라면 `설정 ⚙️` → `파일과 링크` → **`위키링크 사용`을 끄세요.**

### 커밋이 충돌났다고 나올 때

GitHub 웹에서 직접 파일을 고쳤을 때 생깁니다.
`Ctrl + P` → `Pull` 을 먼저 실행한 뒤 다시 커밋하세요.

---

## 9. 이것만은 지키기

- **이미지는 반드시 붙여넣기(`Ctrl+V`)로** — 파일 탐색기에서 끌어다 넣으면 경로가 꼬일 수 있습니다.
- **파일명은 `YYYY-MM-DD_주제`** — 날짜순 정렬이 깨집니다.
- **문서 안 제목은 `##` 부터** — `#` 은 문서 맨 위 제목 하나만.
- **`.obsidian` 폴더는 건드리지 않기** — 설정이 들어 있고, GitHub에는 안 올라갑니다.
